version: '3.8'

services:
  n8n-app:
    image: n8nio/n8n:latest
    restart: always
    ports:
      - "5678:5678"
    volumes:
      - n8n_data:/home/node/.n8n
      - .:/workspace
    environment:
      - N8N_SECURE_COOKIE=false

volumes:
  n8n_data:
